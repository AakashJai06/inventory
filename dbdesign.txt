Got it 👍
Below is the UPDATED DATABASE SCHEMA written specifically for Word (.docx) — table-by-table, clean, academic, and WITHOUT users table, since authentication & role management are external.
You can directly copy–paste this into MS Word.
Each table is formatted exactly how DB schema sections are usually shown in reports.
Database Schema Design
Contextual Content Management System (CCMS)
Table: templates
Description: Stores logical template definitions used to define documentation structure.
Column Name
Data Type
Constraint
Description
template_id
INTEGER
Primary Key
Unique identifier for template
template_name
VARCHAR(100)
UNIQUE, NOT NULL
Name of the template
created_at
TIMESTAMP
NOT NULL
Template creation timestamp
Table: template_versions
Description: Stores versioned template schemas defining hierarchy and validation rules.
Column Name
Data Type
Constraint
Description
template_version_id
INTEGER
Primary Key
Unique template version identifier
template_id
INTEGER
Foreign Key
References templates(template_id)
version_number
INTEGER
NOT NULL
Template version number
schema_json
JSONB
NOT NULL
Template hierarchy and section rules
created_at
TIMESTAMP
NOT NULL
Template version creation time
Table: contents
Description: Represents a logical documentation entity created using a specific template version.
Column Name
Data Type
Constraint
Description
content_id
INTEGER
Primary Key
Unique documentation identifier
template_id
INTEGER
Foreign Key
References templates(template_id)
template_version_id
INTEGER
Foreign Key
Template version bound to content
created_at
TIMESTAMP
NOT NULL
Content creation timestamp
Table: content_versions
Description: Stores different versions of documentation including drafts and published versions.
Column Name
Data Type
Constraint
Description
content_version_id
INTEGER
Primary Key
Unique content version identifier
content_id
INTEGER
Foreign Key
References contents(content_id)
version_number
INTEGER
NOT NULL
Version number of the content
status
VARCHAR(20)
NOT NULL
DRAFT / REVIEW / PUBLISHED / DEPRECATED
content_json
JSONB
NOT NULL
Documentation content
created_at
TIMESTAMP
NOT NULL
Version creation timestamp
📌 Only one active DRAFT is allowed per content (enforced at service level / DB constraint).
Table: content_sections
Description: Stores dynamically created content-level sections and subsections.
Column Name
Data Type
Constraint
Description
section_id
VARCHAR(100)
Primary Key
Unique section identifier
content_id
INTEGER
Foreign Key
References contents(content_id)
section_type
VARCHAR(50)
NOT NULL
Section type defined in template
parent_section_id
VARCHAR(100)
Foreign Key (Self)
Parent section identifier
section_order
INTEGER
NOT NULL
Order within parent section
created_at
TIMESTAMP
NOT NULL
Section creation time
📌 Supports unlimited repeatable sections while enforcing template hierarchy rules.
Table: asset_mappings
Description: Maps application context identifiers to documentation content.
Column Name
Data Type
Constraint
Description
asset_id
VARCHAR(100)
Primary Key
Application context identifier
content_id
INTEGER
Foreign Key
References contents(content_id)
📌 Supports many Asset IDs mapped to a single content.
Table: session_mappings
Description: Maps session identifiers to content sections for contextual navigation.
Column Name
Data Type
Constraint
Description
session_id
VARCHAR(100)
Primary Key
Session identifier
section_id
VARCHAR(100)
Foreign Key
References content_sections(section_id)
content_id
INTEGER
Foreign Key
References contents(content_id)
📌 Session identifiers enable auto-scroll and contextual navigation handled by client UI.
Table: audit_logs
Description: Maintains audit records for documentation lifecycle actions.
Column Name
Data Type
Constraint
Description
audit_id
INTEGER
Primary Key
Unique audit record identifier
content_id
INTEGER
Foreign Key
References contents(content_id)
action
VARCHAR(50)
NOT NULL
Action performed
performed_at
TIMESTAMP
NOT NULL
Action timestamp
🔑 Key Design Notes (Optional section for Word)
Templates define structure rules, not fixed content
Content sections are dynamic and repeatable
Section IDs are content-level
Session IDs reference sections for contextual navigation
Template versions prevent retroactive breaking
Asset IDs enable context-based content retrieval
User and role management is handled by an external system
🧠 One-line Summary (Viva-friendly)
“The CCMS stores template rules, versioned content, dynamic sections, and context mappings, while authentication and authorization are handled externally.”
If you want next, I can:
Convert this into proper SQL DDL
Reduce it to minimum tables for MVP
Add sample rows for demo
Align this exactly with your SRS section numbering
Just tell me 👍